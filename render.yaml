services:
  - type: web
    name: blue-falcon
    runtime: python
    plan: starter
    # Explicitly use Daphne (ASGI server for Django Channels/WebSocket)
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: SECURE_SSL_REDIRECT
        value: "True"
      - key: CSRF_COOKIE_SECURE
        value: "True"
      - key: SESSION_COOKIE_SECURE
        value: "True"
      - key: DATABASE_ENGINE
        value: django.db.backends.sqlite3
      - key: DATABASE_NAME
        value: db.sqlite3
      - key: ALLOWED_HOSTS
        value: "blue-falcon.onrender.com,*.onrender.com"
      - key: WEB_CONCURRENCY
        value: "0"
